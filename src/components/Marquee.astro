---
import Icon from 'astro-icon'
---

<section class="marquee site-section">
  <div class="marquee-container">
    <Icon name="logoipsum-one" />
    <Icon name="logoipsum-two" />
    <Icon name="logoipsum-three" />
    <Icon name="logoipsum-four" />
    <Icon name="logoipsum-five" />
    <Icon name="logoipsum-six" />
    <Icon name="logoipsum-seven" />
    <Icon name="logoipsum-eight" />
  </div>

  <div class="marquee-container">
    <Icon name="logoipsum-one" />
    <Icon name="logoipsum-two" />
    <Icon name="logoipsum-three" />
    <Icon name="logoipsum-four" />
    <Icon name="logoipsum-five" />
    <Icon name="logoipsum-six" />
    <Icon name="logoipsum-seven" />
    <Icon name="logoipsum-eight" />
  </div>
</section>

<style>
  .marquee,
  .marquee-container {
    display: flex;
    gap: var(--section-spacing);
  }

  .marquee {
    margin-bottom: var(--section-spacing);
    overflow-x: hidden;
    mask-image: linear-gradient(
      to right,
      hsl(0 0% 0% / 0),
      hsl(0 0% 0% / 1) 20%,
      hsl(0 0% 0% / 1) 80%,
      hsl(0 0% 0% / 0)
    );
    user-select: none;
  }

  .marquee-container {
    flex: 0 0 auto;
    flex-direction: row;
    align-items: center;
    justify-content: space-evenly;
    min-width: 100%;
    z-index: 1;
    animation: marquee 30s linear 0s infinite;
  }

  .marquee-container [astro-icon] {
    display: inline-block;
    width: 100px;
    height: 100px;
  }

  @keyframes marquee {
    from {
      transform: translateX(0);
    }
    to {
      /* This prevents "skipping" on animation reset */
      transform: translateX(calc(-100% - var(--section-spacing)));
    }
  }
</style>
